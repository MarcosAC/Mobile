import 'package:flutter/material.dart';

class AppColors {
  // static const Color pixelOnColor = Colors.black; // Preto para pixels "ligados"
  // static const Color pixelOffColor = Colors.grey; // Cinza para pixels "desligados"
  // static const Color borderColor = Color(0xFFCCCCCC); // Preto para as bordas

  static const Color pixelOnColor = Colors.black;   // Preto para o bichinho
  static const Color pixelOffColor = Color(0xFFEEEEEE); // Cinza claro para o fundo
  static const Color borderColor = Color(0xFFCCCCCC); // Cinza para bordas

  // static const Color pixelOnColor = Colors.black; // Preto para pixels "ligados"
  // static const Color pixelOffColor = Colors.grey; // Cinza para pixels "desligados"
  // static const Color borderColor = Colors.black; // Preto para as bordas

  static const double borderWidth = 1.0; // Espessura da borda
}
